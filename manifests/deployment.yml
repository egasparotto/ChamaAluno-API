apiVersion : apps/v1
kind: Deployment
metadata:
  name: chamaalunoapi 
spec:
  replicas: 3
  selector:
    matchLabels:
      app: chamaalunoapi
  template:
    metadata:
      labels:
        app: chamaalunoapi 
    spec:
      containers:
        - name: chamaalunoapi 
          image: chamaaluno.azurecr.io/chamaalunoapi
          ports:
          - containerPort: 80
          volumeMounts:
            - mountPath: "/Licenca"
              name: licenca
      volumes:
      - name: licenca
        persistentVolumeClaim:
          claimName: pvc-d6b6be63-2ede-4262-bd36-3ce296a8b254
